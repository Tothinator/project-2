<div id="accordion">
    {{> search }}
    <h3 id="click">Result</h3>
    <div id="result">
        <h1>DON'T FREAK OUT!! RESULTS NOT DISPLAYING NEED TO FIGURE OUT HOW TO RE-RENDER SAME PAGE WITH NEW HBS INFO</h1>

        <h2>Look in the console to see how the webpage should look, having trouble displaying though!</h2>
        {{#if meals}}
            {{#each meals}}
                {{> recipe }}
            {{/each}}
        {{/if}}
    </div>
    <h3 id="recipe">Recipe</h3>
    <div>
       <p>
            <iframe width="100%" height="600" src="" marginwidth="0" marginheight="0" frameborder="0"></iframe>
       </p>
    </div>
</div>

<script type="text/javascript" src="js/form.js"></script>
